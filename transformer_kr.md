### 트랜스포머

간단한 행렬 연산만으로 시퀀스 내 단어들의 상관관계를 효과적으로 포착할 수 있다는 점이 혁신적인 특징으로
트랜스포머 구조는 복잡해보이지만 같은 블록이 반복된다는 점을 이해하면 명확해진다 (임베딩 레이어, 멀티헤드 어텐션, 합 & 정규화, 피드 포워드 네트워크).


1. 데이터 준비
트랜스포머 모델을 학습시키기 위해 가장 먼저 해야 할 일은 데이터셋을 만들어야 한다.
원본 데이터(텍스트, 이미지)를 임베딩으로 변환하는 과정부터 시작한다.
텍스트와 이미지같은 이산 데이터를 저차원 임베딩 벡터로 변환하는 과정은
매우 중요한데, 이렇게 변환된 벡터는 연속적인
공간에 표현되어 미분이 가능해지기 때문이다.
이미지의 경우, 픽셀 좌표 (0,0)과 (0,1)은 물리적으로 인접해 있지만 기본적으로 독립적인 단위
각 픽셀은 별개의 색상값을 가지며 그 자체로는 연속적인
관계를 갖지 않는다. 특시 원시 픽셀 표현에서는 더욱 그렇다.

텍스트 데이터를 다룬다면, 먼저 데이터셋에서 모든 단어를 추출하여 단어장을 만들고, 각 단어에 고유한 숫자 인덱스(토큰 ID) 를 배정한다. 이를 통해 모델이 텍스트 데이터를 숫자로 변환하여 처리할 수 있도록 한다.

기존 모델의 어텐션은 입력 시퀀스와 출력 시퀀스 간의 관계에 주목했지만
셀프 어텐션은 입력 시퀀스 내부에서 단어들 간의 상관관계에 주목한다.

셀프 어텐션 작동방식은
- 동일한 입력 행렬을 서로 다른 네트워크에 통과시켜 Q, K행렬을 생성한다.
- Q와 K행렬을 곱합으로써 단어 간 솽관관계를 계산한다. (Q*K^T 전치 형태로 행렬곱 수행)
- 여기에 V 행렬도 추가되어 최종 출력을 계산한다.


비전 트랜스포머에서도 셀프 어텐션 메커니즘을 이미지에 적용하는 방식이 이와 같아
1. 이미지 패치화: 32*32*3 이미지를 작은 패치들로 분할한다 (4*4)
2. 선형 임베딩: 각 패치를 벡터로 변환한다 (패치 임베딩)
3. 위치 인코딩: 각 패치의 위치 정보를 추가한다. 컨볼루션 과정에서 자연스럽게 위치값을 가질 수 있는, 즉 지역적 특징에 집중할 수 있는 CNN과 달리 트랜스포머는 패치의 위치 정보가 자동으로 보장되지 않는다. 이미지 전체에 걸친 장거리 의존성을 더 잘 포착할 수 있다.
4. 셀프 어텐션 적용:
   - 입력 패치 시퀀스로부터 Q, K, V 행렬 생성. 즉 같은 입력값이지만 이 입력값을 표현하는 방법을 달리 가져간다. 즉 다른 **관점**을 가진다.
   - 입력 X가 있을 때 서로 다른 세 개의 가중치 행렬과 곱해서 Q,K,V 생성
   - Q = X * W_Q (이 토큰/패치가 무엇을 찾고 있는가?)
   - K = X * K_Q (이 토큰/패치가 어떤 정보를 제공하는가?)
   - V = X * V_Q (이 토큰/패치의 실제 내용은 무엇인가?)
   - W_Q, K_Q, V_Q 이 가중치들은 학습 가능한 파라미터로 각각 다른 값을 가지게 된다.
   - Q*K^T 전치 형태로 행렬곱 수행, 이 과정으로 어텐션 점수 (패치 간 관계) 계산
   - 가중치 V 행렬곱하여 최종 어텐션 값 계산
이 방식으로 이미지의 각 부분 (패치)이 다른 부분과 어떻게 관련되는지 모델이 학습할 수 있게 된다.

https://wikidocs.net/31379
