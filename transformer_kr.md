### 트랜스포머

간단한 행렬 연산만으로 시퀀스 내 단어들의 상관관계를 효과적으로 포착할 수 있다는 점이 트랜스포머의 혁신적인 특징이다. 트랜스포머 구조는 복잡해 보이지만 같은 블록(임베딩 레이어, 멀티헤드 어텐션, 합 & 정규화, 피드 포워드 네트워크)들이 반복된다.

#### 1. 데이터 준비

트랜스포머 모델을 학습시키기 위해 가장 먼저 해야 할 일은 데이터셋 준비, 즉 원본 데이터(텍스트, 이미지)를 임베딩으로 변환하는 과정부터 시작

텍스트에서 각 토큰이 서로 독립적인 것처럼 이미지에서도 각 픽셀은 독립적인 단위로 볼 수 있다. 둘 다 이산적인 데이터 형태이며 트랜스포머와 같은 모델에서는 이러한 이산 데이터를 저차원 연속적인 임베딩 벡터로 변환하여 (연속적인 공간에 표현되어) **미분 가능**한 형태로 만들고 이를 통해 신경망 학습이 가능해진다.

* e.g., 픽셀 좌표 (0,0)과 (0,1)은 물리적으로 인접해 있지만 기본적으로 독립적인 단위.
* e.g., 각 픽셀은 별개의 색상값을 가지며 그 자체로는 연속적인 관계를 갖지 않음 (특히 원시 픽셀 표현에서는 더욱 명확함.)

텍스트 데이터를 다룬다면, 먼저 데이터셋에서 모든 단어를 추출하여 단어장을 만들고, 각 단어에 고유한 숫자 인덱스(토큰 ID)를 배정한다. 이를 통해 모델이 텍스트 데이터를 숫자로 변환하여 처리할 수 있도록 한다.

#### 2. 셀프 어텐션

기존 모델의 어텐션은 입력 시퀀스와 출력 시퀀스 간의 관계에 주목했지만 셀프 어텐션은 **입력 시퀀스 내부**에서 단어들 간의 상관관계에 주목한다.

##### 셀프 어텐션 작동방식
* 동일한 입력 행렬을 서로 다른 네트워크에 통과시켜 Q, K, V 행렬을 생성한다.
* Q와 K 행렬을 곱함으로써 단어 간 상관관계를 계산한다. (Q*K^T 전치 형태로 행렬곱 수행)
* 여기에 V 행렬도 추가되어 최종 출력을 계산한다.

#### 3. 비전 트랜스포머

비전 트랜스포머에서도 셀프 어텐션 메커니즘을 이미지에 적용하는 방식은 비슷하다:

##### 1) 이미지 패치화
32×32×3 이미지를 작은 패치들로 분할 (예: 4×4). 이미지를 작은 규격화된 영역으로 나누는 과정으로 CNN의 컨볼루션과 필터와 비슷하지만 **한 번에** **병렬적**으로 전체 이미지를 패치로 나눈다는 점이 다르다.

##### 2) 선형 임베딩
각 패치를 벡터로 변환(패치 임베딩). e.g., 2D 패치는 1D 패치로 평탄화.

##### 3) 포지셔널 인코딩
각 패치의 위치 정보를 추가한다. CNN 컨볼루션 과정에서는 자연스럽게 위치값을 가질 수 있었지만 (지역적 특징에 집중할 수 있는 CNN과 달리) 트랜스포머는 패치의 위치 정보가 자동으로 보장되지 않습니다. 트랜스포머 셀프 어텐션은 순서와 무관하게(permutation invariant) 작동하기 때문에 위치 값을 명시적으로 추가해줘야 합니다. 이를 통해 이미지 전체에 걸친 장거리 의존성을 더 잘 포착할 수 있습니다.

##### 4) 셀프 어텐션 적용: 멀티헤드 셀프 어텐션
인코더 블록의 핵심 요소라 할 수 있습니다.

* 입력 패치 시퀀스로부터 Q, K, V 행렬 생성. 즉 같은 입력값이지만 이 입력값을 표현하는 방법을 달리 가져갑니다. 즉 다른 **관점**을 가집니다.
* 입력 X가 있을 때 서로 다른 세 개의 가중치 행렬과 곱해서 Q, K, V 생성:
  * Q = X * W_Q (나 (현재 토큰/패치)가 어떤 다른 토큰과 관련이 있는지 찾고 있어)
  * K = X * W_K (다른 토큰들이 나 (현재 토큰/패치)를 찾을 때 사용하는 **특성**)
  * V = X * W_V (관계 확인 후 전달할 실제 정보)
* W_Q, W_K, W_V 이 가중치들은 학습 가능한 파라미터로 각각 다른 값을 가지게 됩니다.
* Q*K^T 전치 형태로 행렬곱 수행
* Q와 K는 함께 작동하여 어텐션 맵을 생성합니다. 하나의 토큰의 쿼리와 다른 토큰의 키가 내적되어 관련성 점수가 계산됩니다.
* 이 과정으로 어텐션 점수(패치 간 관계) 계산. 어텐션 점수가 높을수록 해당 밸류 벡터의 정보가 더 많이 반영됩니다.
* 이 점수를 스케일링(내적 결과를 키 벡터 차원의 제곱근으로 나눔)하고 소프트맥스를 적용(모든 점수의 합이 1이 될 수 있는 가중치로 변환)합니다.
* 가중치와 V 행렬을 곱하여 최종적으로 부여된 밸류 벡터들의 합을 출력합니다.
* 여러 개의 어텐션 헤드를 병렬로 사용하여 다양한 관점에서 관계를 포착합니다.
  * 예를 들어 한 헤드는 문법적 관계에 집중, 다른 헤드는 의미적 유사성에 집중, 또 다른 헤드는 문맥적 관계에 집중 등

이미지에서는:
* 한 헤드는 색상 패턴에 집중
* 다른 헤드는 모양이나 윤곽에 집중
* 또 다른 헤드는 텍스처에 집중

#### 4. 셀프 어텐션 예시

"나는 과일 중에서 망고를 좋아한다" 문장에서 망고에 초점을 맞출 때 셀프 어텐션 메커니즘은 망고 토큰이 자기 자신을 포함한 모든 토큰과의 관계를 계산합니다:
* Q_망고 * K_나
* Q_망고 * K_는
* Q_망고 * K_과일
* Q_망고 * K_중에서
* Q_망고 * K_망고 (자기 자신과의 점수 계산)
* Q_망고 * K_를
* Q_망고 * K_좋아한다

재미있는 사실은 자기 자신과의 어텐션 점수가 항상 다른 토큰들보다 높게 나오는 것은 아닙니다. 이는 모델의 학습 과정, 특정 어텐션 헤드의 역할, 그리고 문맥에 따라 크게 달라집니다.

* 일부 어텐션 헤드에서는 자기 자신에게 높은 점수를 부여하는 경향이 있습니다. 이는 토큰의 고유한 의미나 특성을 보존하는데 중요합니다.
* 다른 어텐션 헤드에서는 문맥적으로 관련된 다른 토큰들에 더 높은 점수를 부여합니다. 예를 들어 망고는 '과일'이나 '좋아한다'와 같은 관련 토큰에 더 높은 점수를 줄 수 있습니다.

멀티헤드 어텐션의 효과에 의해, 각 헤드는 서로 다른 패턴을 학습하여 다양한 관계를 포착합니다. 일부 헤드는 자기 자신에게 집중하고, 다른 헤드는 문맥적 관계에 집중할 수 있습니다.

이것을 이미지에 적용하면, 이미지 패치들은 셀프 어텐션을 통해 이미지의 각 부분(패치)이 다른 부분과 어떻게 관련되는지 모델이 학습할 수 있게 됩니다.

즉, 같은 데이터를 여러 관점에서 바라보는 능력이 트랜스포머가 복잡한 패턴을 효과적으로 학습할 수 있게 하는 핵심 요소입니다.

#### 5. 기타 구성요소
* **피드포워드 네트워크**: 각 위치에 독립적으로 적용되는 완전 연결 레이어
* **레이어 정규화 및 잔차 연결**: 학습 안정성 및 기울기 전파 개선

#### 참고자료
[트랜스포머(Transformer)](https://wikidocs.net/31379)
